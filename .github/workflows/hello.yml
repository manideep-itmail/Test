name: My GitHub Action

on:
  push:

jobs:
  hello-job:
    runs-on: ubuntu-latest

    steps:
      - name: Run Shell Script
        run: |
          chmod +x .github/workflows/test.sh
          Test/.github/workflows/test.sh